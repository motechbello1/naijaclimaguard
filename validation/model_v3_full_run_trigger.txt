Model v3 full development trigger — final pre-score protocol

Trigger refreshed after all pre-score safeguards were committed:
- canonical Validation v2 evidence is machine-verified;
- Model v3 registry is frozen at 16 development events / 12 OOF events;
- scientific sanity review is fixed;
- development threshold policy is predeclared;
- freeze-candidate serialization is gated;
- untouched/prospective acceptance policy and one-shot evaluator are predeclared.

Execute only 2018-01-01 through 2021-12-31 development data. Production must remain derived-v2.
