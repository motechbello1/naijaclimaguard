# QA scope: platform language + mobile v2

Verify TypeScript/Prisma after:
- independent `platformLanguage` and outbound `preferredLanguage` fields;
- global curated translation bridge;
- mobile header reduced to menu + compact identity;
- mobile preferences collapsed by default.
