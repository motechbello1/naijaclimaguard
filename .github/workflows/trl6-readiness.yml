name: TRL6 Preparation Contract

on:
  pull_request:
    branches: [main]
    paths:
      - "app/**"
      - "components/**"
      - "lib/**"
      - "prisma/**"
      - "scripts/check-trl6-readiness.mjs"
      - "scripts/check-trl6-claim-gate.mjs"
      - "validation/**"
      - ".github/workflows/trl6-readiness.yml"
      - ".github/workflows/trl6-claim-gate.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repository-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Verify software and evidence-preparation contracts
        run: node scripts/check-trl6-readiness.mjs
